VITE_API_BASE_URL=http://localhost:3001

# SSH 终端配置
# 设置为 true 启用模拟模式（无需后端，用于演示）
# 设置为 false 或注释掉以使用真实模式（需要后端 WebSocket 支持）
VITE_SSH_MOCK_MODE=true

# WebSocket 地址（仅在真实模式下使用）
VITE_WS_URL=ws://localhost:3001
